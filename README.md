How to run the file: 
1. clone the repo using git clone
2. Make a python virtual environment by doing python3 -m venv .venv
3. Select the virtual environment in your code editor
4. run pip install flask mysql-connector-python at command line
5. run python3 app.py and navigate to http://127.0.0.1:5000
6. app should be running
